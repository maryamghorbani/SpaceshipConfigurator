import React from "react";

//import components
import Card from "./UI/Crd";

const Receipt = props => {
    return(
        <div className={props.className}>
            <Card>Receipt</Card>
        </div>
    )
};

export default Receipt;